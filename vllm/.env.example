# Path to your HuggingFace cache so vLLM reuses already-downloaded weights.
# Defaults to ~/.cache/huggingface in docker-compose.yml.
HF_HOME=/home/prog/.cache/huggingface

# Required only for gated repos. Qwen3-32B-AWQ is open, leave blank.
HUGGING_FACE_HUB_TOKEN=
